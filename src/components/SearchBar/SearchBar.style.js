import { StyleSheet } from "react-native";

export default StyleSheet.create({
    searchBarContainer:{
        margin : 8,
        width : '98%',
        height : 65,
        backgroundColor : "#eceff1",
        borderRadius : 10,
        fontSize : 20,
        padding : 10
    },
});